from setuptools import setup, find_packages

setup(
    name="slim_goodie",
    version="0.0.3",
    packages=find_packages(),
    install_requires=[
        # Add dependencies here,
        # 'pytest',
    ],
    entry_points={
        "console_scripts": [
        ],
    },
)
